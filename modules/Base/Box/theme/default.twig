{% if logged == false %}
    {{ status | raw }}
    {{ login_form | raw }}
{% else %}

    <nav class="navbar navbar-default">
        <div class="container-fluid">
            <!-- Brand and toggle get grouped for better mobile display -->
            <div class="navbar-header">
                <a class="navbar-brand" {{ home.href | raw }}><i class="fa fa-home"></i>&nbsp;{{ home.label }}</a>
                {#{{ logo | raw }}#}
            </div>

            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                <ul class="nav navbar-nav">
                    {{ menu | raw }}
                </ul>
                <p class="navbar-text">{{ moduleindicator | raw }}</p>
{#                <form class="navbar-form navbar-left" role="search">
                    <div class="form-group">
                        <input type="text" class="form-control" placeholder="Search">
                    </div>
                    <button type="submit" class="btn btn-default">Submit</button>
                </form>#}
                {#{{ search | raw }}#}
                <ul class="nav navbar-nav navbar-right">
                    <li style="margin-right: 30px">{{ actionbar | raw }}</li>
                    <li><button type='button' data-toggle='modal' data-target='#launchpadModal' class="btn btn-default navbar-btn"><i class="fa fa-cubes"></i>&nbsp;Launchpad</button></li>
                    {{ login | raw }}
                </ul>
            </div>
            <!-- /.navbar-collapse -->
        </div>
        <!-- /.container-fluid -->
    </nav>

<div class="container-fluid">
        {{ main | raw}}
 </div>

<footer class="footer">
    <div class="container-fluid">
        <p class="text-muted">
            <a href="http://epe.si" target="_blank"><b>EPESI</b> powered</a>
            <span style="float: right">{{ version_no | raw }}</span>
            {% if donate %}
                <span style="float: right; margin-right: 30px">{{ donate | raw }}</span>
            {% endif %}
        </p>
    </div>
</footer>

    {{ launchpad_modal | raw}}
    {{ filter | raw}}
    {{ status | raw}}

<style>
    html {
        position: relative;
        min-height: 100%;
    }

    body {
        /* Margin bottom by footer height */
        margin-bottom: 60px;
    }
    .footer {
        position: absolute;
        bottom: 0;
        width: 100%;
        /* Set the fixed height of the footer here */
        height: 60px;
        background-color: #f5f5f5;
    }

    .footer .container-fluid {
        width: auto;
        padding: 0 15px;
    }
    .footer .container-fluid .text-muted {
        margin: 20px 0;
    }
</style>

{% endif %}